# UL2755 Sprint 測試計畫（2026-04-21）

**目標覆蓋率**：整體 > 95%（srs.js 100% 分支、app.js ≥ 95%、sw.js ≥ 90%、關鍵流程 E2E 100%）

---

## 1. 範圍

### 1.1 納入測試
| 模組 | 檔案 | 測試重點 |
|---|---|---|
| 純邏輯 | `srs.js` | SM-2 演算法、Deck 生成、課表、時間函式 |
| UI 組合 | `app.js` | 儲存層、路由、Dashboard / Calendar / Day / Settings 渲染、兩模式流程 |
| 結構 | `index.html` | 關鍵 DOM id / class 存在、CSS 視覺不破版 |
| PWA | `sw.js`, `manifest.webmanifest` | 安裝、快取、離線 |
| 資料 | `questions.json`, `audio/*.mp3` | schema 正確、音檔可取得 |

### 1.2 排除（已知限制、手動驗證）
- 藍牙耳機實機播放音質（只能人耳判斷）
- iOS native share sheet
- 跨 GitHub Pages 部署延遲
- 使用者實體安裝 PWA 至主畫面的 OS 提示

---

## 2. 測試層級

| 層 | 代號 | 執行方式 | 覆蓋目標 |
|---|---|---|---|
| L1 Unit | TC-U-*** | `srs.test.html`（瀏覽器內 assert） | 100% srs.js 分支 |
| L2 Integration | TC-I-*** | `preview_eval` 在瀏覽器 context 跑 | 儲存層、migration、router |
| L3 DOM Render | TC-D-*** | `preview_eval` 讀 `querySelector` | 每個 render 函式關鍵節點 |
| L4 End-to-End | TC-E-*** | `preview_eval` 互動序列 | Quiz / Riding / Settings / Calendar 完整流程 |
| L5 Manual | TC-M-*** | 人工手機/桌面驗證 | 音效、震動、PWA、離線 |
| L6 Regression | TC-R-*** | 每次 release 前跑一次 | 防回歸 |

---

## 3. 單元測試（TC-U-***）— srs.js

### 3.1 nextState() — 11 case 已實作，**補 4 case 至完整分支**

| ID | Case | Input | Expected |
|---|---|---|---|
| TC-U-001 | new → correct | `(null, true, t0)` | `stage=learning, due=t0+10m` |
| TC-U-002 | new → wrong | `(null, false, t0)` | `stage=new, cc=0` |
| TC-U-003 | learning → correct → review 1d | learning record, correct | `stage=review, iv=1440` |
| TC-U-004 | learning → wrong stays learning | learning record, wrong | `stage=learning, iv=10` |
| TC-U-005 | review cc=0 → cc=1 iv=3d | review(0,1440), correct | `cc=1, iv=4320` |
| TC-U-006 | review cc=1 → cc=2 iv=7d | review(1,4320), correct | `cc=2, iv=10080` |
| TC-U-007 | review cc=2 → graduated 30d | review(2,10080), correct | `stage=graduated, cc=3, iv=43200` |
| TC-U-008 | review → wrong → learning | review(2,10080), wrong | `stage=learning, cc=0, iv=10` |
| TC-U-009 | graduated 30d → 90d | graduated(43200), correct | `iv=129600` |
| TC-U-010 | graduated 180d → 180d 封頂 | graduated(259200), correct | `iv=259200` |
| TC-U-011 | graduated → wrong → review 1d | graduated, wrong | `stage=review, cc=0, iv=1440` |
| **TC-U-012 新** | **undefined record with correct → learning** | `(undefined, true, t0)` | 等同 new→correct |
| **TC-U-013 新** | **total_seen 累加** | record.total_seen=5, 再答 | `total_seen=6` |
| **TC-U-014 新** | **total_correct 僅答對時加** | correct two times then wrong | `total_correct=2` |
| **TC-U-015 新** | **graduated 90d → 180d** | graduated(129600), correct | `iv=259200` |

### 3.2 buildDeck() — 5 已實作，**補 3 case**

| ID | Case | Expected |
|---|---|---|
| TC-U-016 | 空 state + cap 2 | 取 2 new |
| TC-U-017 | 2 due + 1 new cap | `[due1, due2, new1]` 順序 |
| TC-U-018 | session_cap 截尾 | 總長 = cap |
| TC-U-019 | new_taken_today 扣配額 | 取 = per_day - taken |
| TC-U-020 | 未到期 review 不入 deck | 只回未 due 以前 |
| **TC-U-021 新** | **全部 graduated + 無 new → deck 空** | `deck.length === 0` |
| **TC-U-022 新** | **new_per_day = 0 → 不灌新題** | 僅 due queue |
| **TC-U-023 新** | **questions 空陣列** | `deck = []` |

### 3.3 summary() — 1 已實作，**補 3 case**

| ID | Case | Expected |
|---|---|---|
| TC-U-024 | 混合狀態計數 | stage & due 計數正確 |
| **TC-U-025 新** | **全部 new** | `new=N, due=N, learning/review/graduated=0` |
| **TC-U-026 新** | **未來 due 不計入 due** | due_at > now 跳過 |
| **TC-U-027 新** | **graduated 未到期不計入 due** | graduated + future due → due=0 |

### 3.4 migrate() — 2 已實作，**補 2 case**

| ID | Case | Expected |
|---|---|---|
| TC-U-028 | answered correct | `stage=learning, total_correct=1` |
| TC-U-029 | empty → empty | `{}` |
| **TC-U-030 新** | **wrongPool id 重疊 answered** | 以 answered 優先，不重複 |
| **TC-U-031 新** | **null inputs** | 回傳 `{}` 不 crash |

### 3.5 buildCurriculum() — 2 已實作，**補 3 case**

| ID | Case | Expected |
|---|---|---|
| TC-U-032 | 5 Qs / per_day 2 → 3 天 | 正確切分 |
| TC-U-033 | UL → NEC → FXX 排序 | 來源群組優先 |
| **TC-U-034 新** | **per_day 大於題數** | 只 1 天 |
| **TC-U-035 新** | **questions 空** | days = [] |
| **TC-U-036 新** | **同 category 穩定依 id** | 相同 source+cat 下 id 字母序 |

### 3.6 getDayForQuestion / completedDays — 2 已實作，**補 2 case**

| ID | Case | Expected |
|---|---|---|
| TC-U-037 | 找到 / 找不到 | 回日期 / null |
| TC-U-038 | 全看過的日期為 completed | set 包含該日 |
| **TC-U-039 新** | **curriculum null 時** | `getDayForQuestion → null`, `completedDays → empty Set` |
| **TC-U-040 新** | **stage=new 不算 completed** | 即使 total_seen>0 但 stage='new' 不計入 |

### 3.7 computeFinishedYmd / computeNewPerDay（新增，無現有測試）

| ID | Case | Expected |
|---|---|---|
| **TC-U-041 新** | **30 Qs / 10 perDay / start=2026-04-21** | 回 `2026-04-23` |
| **TC-U-042 新** | **finish=2026-04-30 / 30 Qs / start=04-21** | `new_per_day=3` |
| **TC-U-043 新** | **remaining=0** | `finish = start`（至少 1 天）|
| **TC-U-044 新** | **newPerDay=0** | 視為 1，不除零 |
| **TC-U-045 新** | **finish < start** | 退為 1 天 |
| **TC-U-046 新** | **反函式一致**（forward→backward） | `computeNewPerDay(computeFinishedYmd(x,r,s),r,s) === x` 對 x ∈ 常用 |

---

## 4. 整合測試（TC-I-***）— SrsStore & 路由

### 4.1 Storage 往返

| ID | Case | 驗證 |
|---|---|---|
| TC-I-001 | saveSettings → loadSettings | 物件深度相等 |
| TC-I-002 | saveState → loadState | 多筆記錄正確 |
| TC-I-003 | ensureCurriculum 穩定快取 | 第 2 次呼叫無重建 |
| TC-I-004 | ensureCurriculum 重建（per_day 改變） | 新 curriculum 取代 |
| TC-I-005 | ensureCurriculum 重建（題目數量改變） | 新 curriculum 取代 |
| TC-I-006 | incNewToday 跨日歸零 | 模擬系統時鐘隔日 → count=0 |

### 4.2 Migration

| ID | Case | Setup | Expected |
|---|---|---|---|
| TC-I-007 | 舊 answered → 新 state | `localStorage.answered={...}` | srs_state 有對應記錄，old key removed |
| TC-I-008 | 已有 srs_state → 不再遷移 | both keys set | srs_state 不變 |
| TC-I-009 | 兩 key 都空 | fresh install | 不會產生空 srs_state |

### 4.3 路由

| ID | Case | Input | Expected |
|---|---|---|---|
| TC-I-010 | `#home` 渲染 Dashboard | `location.hash='home'` | `.dashboard` 存在 |
| TC-I-011 | `#calendar` 渲染 Calendar | `location.hash='calendar'` | `.cal-grid` 存在 |
| TC-I-012 | `#day=1` 渲染 Day | `location.hash='day=1'` | `.day-card` 存在 |
| TC-I-013 | 未知 hash → fallback home | `location.hash='xyz'` | Dashboard 被渲染 |
| TC-I-014 | 瀏覽器返回鍵 | `history.back()` after nav | 回到上一個 hash 畫面 |

---

## 5. DOM 渲染測試（TC-D-***）

### 5.1 Dashboard

| ID | 驗證 |
|---|---|
| TC-D-001 | `.brand` 文字為 `🏠 Home` |
| TC-D-002 | `.modes` 元素**不存在**（已移除） |
| TC-D-003 | `Projected: YYYY-MM-DD` 格式可 parse |
| TC-D-004 | `Day X of Y` 與 `curriculum.days.length` 一致 |
| TC-D-005 | 三個 `.metric-list li` 分別顯示 Due/New/Graduated 數字 |
| TC-D-006 | 四顆 action button: Quiz, Riding, Calendar, Settings |
| TC-D-007 | progress bar width = todayDone/todayGoal * 100% |

### 5.2 Calendar

| ID | 驗證 |
|---|---|
| TC-D-008 | 42 cells（6 週 × 7 天）均渲染 |
| TC-D-009 | 今天的 cell 有 `.today` class |
| TC-D-010 | 每 in-month + curriculum 日有 `.cell-top`（含 `.date`、`.day-num`） |
| TC-D-011 | 每 curriculum 日有 `.qcount`（格式 `NQ`） |
| TC-D-012 | empty cell 無 cursor pointer（`.cal-cell.empty`） |
| TC-D-013 | `.cal-cell` computed padding 為 `2px`（iOS overflow fix 驗證） |
| TC-D-014 | 月份導覽：`calNav(-1)` 標題變上月 |

### 5.3 Day Page

| ID | 驗證 |
|---|---|
| TC-D-015 | `h2` 文字 `Day N` |
| TC-D-016 | 卡片數 = `day.question_ids.length` |
| TC-D-017 | 每卡片包含 `.q`, `.ans`, `.expl`，**不含** `.opt` / `.aopt` |
| TC-D-018 | 每卡片有 `.sbadge` 狀態徽章 |
| TC-D-019 | 兩顆 action button：Riding this day, Quiz this day |
| TC-D-020 | Back 按鈕 `location.hash='calendar'` |

### 5.4 Settings Modal

| ID | 驗證 |
|---|---|
| TC-D-021 | `#newPerDay` 存在 |
| TC-D-022 | `#finishedDate` 存在（type=date）|
| TC-D-023 | `#sessionCap` **不存在**（已移除） |
| TC-D-024 | Reset 按鈕文字 `Reset learning records` |
| TC-D-025 | 開啟時 `#finishedDate` 有值（非空） |

---

## 6. End-to-End 流程（TC-E-***）

### 6.1 Quiz 流程

| ID | 步驟 | 期望 |
|---|---|---|
| TC-E-001 | 清儲存 → Quiz → 答對第 1 題 | srs_state 有記錄，stage=learning |
| TC-E-002 | 答對後進度前進 | `.question` 切至下一題 |
| TC-E-003 | 答錯觸發紅色震動動畫 | `.shake-no` class 出現 |
| TC-E-004 | Combo ≥ 3 觸發 burst 音 | combo counter 顯示 |
| TC-E-005 | 完成整個 deck → 顯示 Round Complete | `Back to Dashboard` 按鈕可見 |

### 6.2 Riding 流程

| ID | 步驟 | 期望 |
|---|---|---|
| TC-E-006 | 進 Riding → 介紹頁 | `Start` 按鈕可見 |
| TC-E-007 | Start → 播放第 1 題 | audio 元素 `src` 含 `/audio/.../q.mp3` |
| TC-E-008 | 調整 speed 0.5x | `audio.playbackRate = 0.5` 維持 |
| TC-E-009 | Stop → 回介紹頁 | Start 按鈕再出現 |
| TC-E-010 | 播完一輪自動重洗 | `🔄 Lap 2` 提示出現 |

### 6.3 Calendar → Day → Mode

| ID | 步驟 | 期望 |
|---|---|---|
| TC-E-011 | Calendar tap Day 1 | `#day=1` 載入 10 題卡片 |
| TC-E-012 | Day → Quiz this day | 只有該日 10 題 |
| TC-E-013 | Day → Riding this day | audio deck 為該日題目 |
| TC-E-014 | 偷跑：tap Day 3 未到期 | 可進入並練習，答對計入 SRS |

### 6.4 Settings 流程

| ID | 步驟 | 期望 |
|---|---|---|
| TC-E-015 | new_per_day 改為 5 → `#finishedDate` 即時更新 | 為 today+5 天 |
| TC-E-016 | finishedDate 改為 today+10 → `#newPerDay` 即時更新 | 為 `ceil(30/10)=3` |
| TC-E-017 | Save → `Rebuild curriculum?` 彈窗 | `confirm` 被呼叫 |
| TC-E-018 | 彈窗 OK（Keep）→ start_date 保留 | curriculum.start_date 不變 |
| TC-E-019 | 彈窗 Cancel（Reset）→ start_date 設為今天，srs_state 清空 | 所有記錄消失 |
| TC-E-020 | Reset learning records 按鈕 | srs_state / new_today 清空 |

### 6.5 Home 導覽共用（regression TC-E-025~029）

| ID | 步驟 | 期望 |
|---|---|---|
| **TC-E-025** | Dashboard → `enterRiding()` → 點 `.brand` | 返回 Dashboard（已驗證 bug 情境）|
| **TC-E-026** | Dashboard → `enterQuiz()` → 點 `.brand` | 返回 Dashboard |
| **TC-E-027** | Calendar tap day=1 → `enterRidingDay(1)` → 點 `.brand` | 返回 Dashboard |
| **TC-E-028** | Calendar tap day=2 Day 頁 → 點 `.brand` | 返回 Dashboard |
| **TC-E-029** | 任一 mode 點 `.brand` → `stopAudioCleanup()` 被呼叫，audio 暫停 | `sharedAudio.paused === true` |

**設計備忘**：`enterRiding` / `enterQuiz` / `enterRidingDay` / `enterQuizDay` 不改變 URL hash（它們直接改 DOM），因此當 hash 已為 `#home` 時，設定 `location.hash = 'home'` 不會觸發 `hashchange`。`.brand` click handler 必須**強制**呼叫 `renderDashboard()` 才能可靠返回。

### 6.6 PWA & Offline

| ID | 步驟 | 期望 |
|---|---|---|
| TC-E-021 | 首次載入 → SW 註冊成功 | `navigator.serviceWorker.controller` 非空 |
| TC-E-022 | 重載 → `CACHE === 'ul2755-v10'` | cache key 正確 |
| TC-E-023 | 關閉網路 → 重載 | UI 仍可用（快取 shell 命中）|
| TC-E-024 | 點 Riding → MP3 從快取播放 | 無網請求 |

---

## 7. 手動 / 裝置測試（TC-M-***）

### 7.1 iOS 28 Chrome（主要目標）

| ID | 驗證 |
|---|---|
| TC-M-001 | 加到主畫面（長按分享 → Add to Home）| 圖示顯示 🏠，名稱 UL2755 |
| TC-M-002 | 靜音開關推到響鈴 → 騎車模式有聲 | ✅ |
| TC-M-003 | Calendar 格子文字不截斷（D1、10Q、日期都可見） | ✅ |
| TC-M-004 | 藍牙耳機連線播放連續 30 分鐘 | 無斷線 |
| TC-M-005 | 答題震動無效（iOS 限制，但 `.shake-no` 視覺生效）| 預期行為 |
| TC-M-006 | 雙向連動在 `<input type="date">` 上正常 | ✅ |

### 7.2 Android Chrome

| ID | 驗證 |
|---|---|
| TC-M-007 | 震動正常觸發 | ✅ |
| TC-M-008 | PWA install prompt | 顯示安裝提示 |
| TC-M-009 | Service Worker 更新時自動載入新版 | ✅ |

### 7.3 桌面瀏覽器（Chrome / Edge）

| ID | 驗證 |
|---|---|
| TC-M-010 | DevTools 切 iPhone 模擬寬 → UI 不破版 | ✅ |
| TC-M-011 | 系統深色模式自動套用 | 背景維持深藍 |

---

## 8. 迴歸測試（TC-R-***）

**每次 release 前必跑**的精簡清單（20 分鐘內）：

| ID | 項目 | 對應 TC |
|---|---|---|
| TC-R-001 | 清 state → 首頁 Dashboard 顯示 | TC-D-001..007 |
| TC-R-002 | Calendar 首月 3 格高亮 | TC-D-008..012 |
| TC-R-003 | Day 1 Quiz 答對 1 題 | TC-E-001 |
| TC-R-004 | Settings 雙向連動 + 儲存 Keep 分支 | TC-E-015..018 |
| TC-R-005 | Riding 播放一題完整 | TC-E-007 |
| TC-R-006 | 離線載入 | TC-E-023 |
| TC-R-007 | srs.test.html 全數 pass | 全部 TC-U-*** |

---

## 9. 測試資料與設定

### 9.1 Fixture
- `questions.json` 30 題為穩定 fixture
- 測試專用 state：`localStorage.srs_state = JSON.stringify({...})` 手動注入
- 假時鐘：用 Chrome DevTools 將系統時間前推；不採用 monkey-patch `Date.now`

### 9.2 清理
- 每個 TC 執行前：`localStorage.clear()` 確保獨立
- 失敗後：console 擷取 + localStorage dump 附於 bug 單

---

## 10. 工具與執行

| 工具 | 用途 |
|---|---|
| `srs.test.html` + `<script>` assert | L1 單元測試 runner |
| `preview_eval` (MCP) | L2/L3/L4 在主對話中動態執行 |
| Chrome DevTools | L5 裝置模擬、時鐘調整、Network throttle |
| `Lighthouse` | PWA 分數 ≥ 90、Accessibility ≥ 90 |
| `coverage` via V8 | 取得覆蓋率實際百分比（開 DevTools → Coverage tab → Start → Reload → 操作 → Stop） |

---

## 11. 覆蓋率計算

| 檔案 | 總 LOC | 目標覆蓋 | 檢核方式 |
|---|---|---|---|
| `srs.js` | ~200 | **100%** | 46 unit TCs 覆蓋所有分支 |
| `app.js` | ~700 | **≥ 95%** | L2–L4 整合 + E2E 覆蓋；V8 Coverage 驗證 |
| `sw.js` | ~35 | **≥ 90%** | 線上 online + offline 都測 |
| `index.html` CSS | n/a | 視覺 OK | Lighthouse + 裝置實測 |
| 整體 | ~950 | **> 95%** | V8 Coverage 報告匯總 |

### 驗證步驟
1. 跑完全部 TC-U → `srs.test.html` 顯示 `46 passed, 0 failed`
2. Chrome DevTools → Coverage → Reload → 操作全部 E2E → 報告匯出
3. `srs.js` 紅色（未執行）行 = 0
4. `app.js` 紅色行 ≤ 5%（允許 DOM 安全分支、iOS fallback）
5. Lighthouse PWA 分數 ≥ 90

---

## 12. 驗收標準

- [ ] 46 unit TCs 全部 pass
- [ ] 25 DOM render TCs 全部 pass
- [ ] 29 E2E TCs 全部 pass（含新增 TC-E-025~029 Home 導覽）
- [ ] iOS 28 Chrome 6/6 manual 通過
- [ ] Android Chrome 3/3 manual 通過
- [ ] 桌面 2/2 通過
- [ ] Lighthouse PWA ≥ 90
- [ ] V8 Coverage 整體 > 95%
- [ ] Regression 7 項每次 release 前執行

---

## 13. 風險與 Out-of-Test

- 音質評估（主觀）— 人耳抽樣 3 題/更新。
- 系統時鐘跨時區 — 已修 local YMD bug；建議加 TC-U-047 驗證 UTC−8 / UTC+8 兩邊界。
- localStorage 配額爆滿 — 目前 ~60 KB 遠低於 5 MB 門檻；未來題庫擴到 220 時重估。
- 網路不穩 MP3 失敗 — TC-E-008 中 Audio `onerror` fallback 目前只 log；需加 UI 提示（Issue 待追）。

---

## 14. 實施順序建議

1. 先補 srs.test.html 至 46 TCs（半小時）→ 取得 100% 純邏輯覆蓋
2. 寫 `app.test.html`（或共用 srs.test.html）跑 L2/L3/L4 的 preview_eval 序列
3. 跑 V8 Coverage 定位未測路徑
4. 補缺口 TCs 至 > 95%
5. iOS + Android 手動全套驗收
6. 形成 regression checklist 至 CI / release 腳本

